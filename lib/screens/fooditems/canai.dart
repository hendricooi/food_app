import 'package:flutter/material.dart';

class Canai extends StatelessWidget {
  const Canai({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Roti Canai"),
      ),
    );
  }
}
