import 'package:flutter/material.dart';

class GCB extends StatelessWidget {
  const GCB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("GCB"),
      ),
    );
  }
}
