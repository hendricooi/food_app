import 'package:flutter/material.dart';

class MeatPizza extends StatelessWidget {
  const MeatPizza({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Meat Mania Pizza"),
      ),
    );
  }
}
