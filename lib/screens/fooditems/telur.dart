import 'package:flutter/material.dart';

class Telur extends StatelessWidget {
  const Telur({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Roti Telur"),
      ),
    );
  }
}
