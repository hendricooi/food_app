import 'package:flutter/material.dart';

class Double extends StatelessWidget {
  const Double({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Double Regular Scoop Ice Cream"),
      ),
    );
  }
}
