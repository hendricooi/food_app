import 'package:flutter/material.dart';

class CaramelLattee extends StatelessWidget {
  const CaramelLattee({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Caramel Lattee"),
      ),
    );
  }
}
