import 'package:flutter/material.dart';

class Mocha extends StatelessWidget {
  const Mocha({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("The Orginal Mocha"),
      ),
    );
  }
}
