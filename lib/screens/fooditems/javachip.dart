import 'package:flutter/material.dart';

class JavaChip extends StatelessWidget {
  const JavaChip({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Java Chip Frappuccino"),
      ),
    );
  }
}
