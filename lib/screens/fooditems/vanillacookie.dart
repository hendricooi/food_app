import 'package:flutter/material.dart';

class Vanilla extends StatelessWidget {
  const Vanilla({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vanilla Cookies & Cream Ice Blended"),
      ),
    );
  }
}
