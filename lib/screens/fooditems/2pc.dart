import 'package:flutter/material.dart';

class twopiece extends StatelessWidget {
  const twopiece({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("2-pc Combo"),
      ),
    );
  }
}
