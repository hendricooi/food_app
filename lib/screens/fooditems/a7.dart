import 'package:flutter/material.dart';

class A7 extends StatelessWidget {
  const A7({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Signature Jade Jelly"),
      ),
    );
  }
}
