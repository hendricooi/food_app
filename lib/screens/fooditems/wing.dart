import 'package:flutter/material.dart';

class Wing extends StatelessWidget {
  const Wing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Roasted Wing Chicken Rice"),
      ),
    );
  }
}
