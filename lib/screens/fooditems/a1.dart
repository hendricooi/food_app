import 'package:flutter/material.dart';

class A1 extends StatelessWidget {
  const A1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("BlackBall Signature(Ice Base)"),
      ),
    );
  }
}
