import 'package:flutter/material.dart';

class Sausage extends StatelessWidget {
  const Sausage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Roasted Sausage Rice"),
      ),
    );
  }
}
