import 'package:flutter/material.dart';

class GreeTeaFrap extends StatelessWidget {
  const GreeTeaFrap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Green Tea Frappaccino"),
      ),
    );
  }
}
