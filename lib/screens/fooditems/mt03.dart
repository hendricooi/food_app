import 'package:flutter/material.dart';

class MT03 extends StatelessWidget {
  const MT03({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("MT03"),
      ),
    );
  }
}
