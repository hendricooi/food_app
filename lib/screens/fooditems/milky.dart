import 'package:flutter/material.dart';

class Milky extends StatelessWidget {
  const Milky({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("MTY Milky"),
      ),
    );
  }
}
