import 'package:flutter/material.dart';

class Bawang extends StatelessWidget {
  const Bawang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Roti Telur Bawang"),
      ),
    );
  }
}
