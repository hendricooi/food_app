import 'package:flutter/material.dart';

class ExtraPizza extends StatelessWidget {
  const ExtraPizza({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Extravaganzza Pizza"),
      ),
    );
  }
}
