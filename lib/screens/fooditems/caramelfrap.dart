import 'package:flutter/material.dart';

class CaramelFrap extends StatelessWidget {
  const CaramelFrap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Caramel Frappuccino"),
      ),
    );
  }
}
