import 'package:flutter/material.dart';

class McChicken extends StatelessWidget {
  const McChicken({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("McChicken"),
      ),
    );
  }
}
