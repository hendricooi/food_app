import 'package:flutter/material.dart';

class threepc extends StatelessWidget {
  const threepc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("3-pc Combo"),
      ),
    );
  }
}
