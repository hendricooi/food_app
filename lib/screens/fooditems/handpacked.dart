import 'package:flutter/material.dart';

class HandPacked extends StatelessWidget {
  const HandPacked({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Handpacked Quart"),
      ),
    );
  }
}
