import 'package:flutter/material.dart';

class Penang extends StatelessWidget {
  const Penang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Penang Bihun"),
      ),
    );
  }
}
