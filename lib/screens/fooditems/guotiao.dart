import 'package:flutter/material.dart';

class GuoTiao extends StatelessWidget {
  const GuoTiao({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Guo Tiao Tang"),
      ),
    );
  }
}
