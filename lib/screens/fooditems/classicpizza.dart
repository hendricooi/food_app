import 'package:flutter/material.dart';

class ClassicPizza extends StatelessWidget {
  const ClassicPizza({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Classic Chicken Pizza"),
      ),
    );
  }
}
